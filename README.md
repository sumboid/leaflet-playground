# Leaflet playground
Let's render rectangles on the map, just because we can.

# Prerequirements
* `nvm i && nvm use`
* `yarn install`

# Development
* `yarn start`

# Build
* `yarn build`

# Linting / formatting
* `yarn fix`
